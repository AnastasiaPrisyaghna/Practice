fn main() {
    let x: i32 = 5; // ініціалізуємо x
    let _y: i32;     // не використовується, можна залишити для попередження

    assert_eq!(x, 5);  // перевірка значення x
    println!("Success!");
}
